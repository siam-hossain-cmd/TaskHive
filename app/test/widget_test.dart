import 'package:flutter_test/flutter_test.dart';
import 'package:taskhive/app.dart';

void main() {
  testWidgets('App starts', (WidgetTester tester) async {
    // Placeholder test — Firebase must be mocked for widget tests.
  });
}
